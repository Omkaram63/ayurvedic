# Legacy Python Version

This folder preserves the original Flask implementation that was previously at the project root.

Files kept here:

- `app.py`
- `model.py`
- `requirements.txt`
- `templates/`
- cached/session artifacts copied from the old structure

The active project now lives in:

- `frontend/` for React
- `backend/` for Node.js
